class_name Game
extends Node2D

const WINDOW_WIDTH := 256
const WINDOW_HEIGHT := 256
const SCREEN_CENTER := Vector2(WINDOW_WIDTH, WINDOW_HEIGHT) / 2

# TODO: Delete bullets that aren't in the playable area
